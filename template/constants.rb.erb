# frozen_string_literal: true

module <%= @module_name %>
	::<%= @short_module_name %> = ::<%= @module_name %>

	APP_DIRS = %w[
		lib
		config
		models
		# policies
		helpers
		# mailers
		# actions
		forms
		# view_objects
		controllers
	].freeze
end
