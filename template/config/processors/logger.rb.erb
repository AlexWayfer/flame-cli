# frozen_string_literal: true

## Logger configuration and methods
module <%= @module_name %>
	module ConfigProcessors
		## Logger configuration
		class Logger
			def initialize(config)
				@config = config

				@config[:logs_dir] = proc do
					File.join(
						self[:root_dir], self[:server][self[:environment]][:logs_dir]
					)
				end
			end
		end
	end
end
