:root {
	--header-logo-height: 66px;
	--header-vertical-padding: 15px;
	--mobile-header-logo-height: 42px;
	--mobile-header-vertical-padding: 7px;
}

body > header {
	background: var(--accent-background-color);
	padding: var(--mobile-header-vertical_padding) 0;
	z-index: 7;

	&,
	a {
		color: black;
	}
}
